import { StyleSheet, View } from 'react-native'
import React from 'react'
import CustomText from '../components/CustomText'

const CreateEvent = () => {
  return (
    <View>
      <CustomText>CreateEvent</CustomText>
    </View>
  )
}

export default CreateEvent

const styles = StyleSheet.create({})